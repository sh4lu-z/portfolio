# Icons Directory

Place your custom icon files (SVG, PNG) here. The HTML file currently uses emoji placeholders for simplicity.
You can replace the emojis in `index.html` with `<img>` tags pointing to files in this directory, e.g., `<img src="/icons/twitter.svg" alt="Twitter">`.
