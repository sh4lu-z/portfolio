# Images Directory

Place your actual image files here. The HTML file currently uses `picsum.photos` placeholders.
Replace the `src` attributes in `index.html` to point to files in this directory, e.g., `/images/profile.jpg`.
